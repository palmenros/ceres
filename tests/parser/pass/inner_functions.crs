fn main() {
    fn max(a: i32, b: i32) i32 {
        if a > b {
            return a;
        } else {
            return b;
        }
    }

    var a : i32 = 2;
    var b : i32 = 3;

    const m : i32 = max(a, b);
}